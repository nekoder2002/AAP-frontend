import { http } from "@/utils/http"
import { setToken, getToken, removeToken, isAuth } from "@/utils/token"
import { history } from "@/utils/history"

export { http, history, setToken, getToken, removeToken, isAuth }